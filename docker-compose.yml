services:
  db:
    container_name: db
    image: postgres
    ports:
      - "5432:5432"
    env_file:
      # shared env file with strapi application
      - ./.env
    volumes:
      - ./db-data/:/var/lib/postgresql/data
    restart: unless-stopped